name: first workflow
on: 
  push:
     branches: [ main ]
jobs:
  demo:
   runs-on: ubuntu-latest  
   
   steps: 
     - name: greeting
      run: echo "Hello sir "

     - uses: actions/checkout@v3 
  
