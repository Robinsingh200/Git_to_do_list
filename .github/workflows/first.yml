name: first workflow
on: 
  push:
     branches: [ main ]
jobs:
  demo:
   runs-on: ubuntu-latest  
   
   steps: 
     - uses: actions/checkout@v3
     
     - name: npm install
       uses: action/setup-node@v3
       with:
         node-version: 18 

     - run: npm install  
     - run: npm test
