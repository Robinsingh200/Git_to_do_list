name: first workflow
on: 
  push:
     branches: [ main ]
jobs:
  test_download:
   runs-on: ubuntu-latest  
   
   steps: 
     - uses: actions/checkout@v3
     
     - name: load file
       run: ls

     - name: done file
       run: echo "file loaded in this git" 
     
     # - name: Install dependencies
     #   uses: actions/setup-node@v3
     #   with:
     #     node-version: 18 

     # - run: npm install  
     # - run: npm test
