name: first workflow
on: 
  push:
     branches: [ main ]
jobs:
  demo:
   runs-on: ubuntu-latest  
   
   steps: 
     - name: greeting
     -uses: actions/checkout@v3
     -run: echo "Hello sir "
  
